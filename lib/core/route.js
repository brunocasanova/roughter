var debug = require( 'debug' )( 'rougther:lib:core:route' ); debug( 'loaded!' );

// Dependencies
var Util = require( 'findhit-util' );
var defaults = require( '../defaults' );

var Router = require( './router' );
